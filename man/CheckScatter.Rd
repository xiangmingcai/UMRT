% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CheckScatter.R
\name{CheckScatter}
\alias{CheckScatter}
\title{Scatter Plot with Optional Scaling and Sampling}
\usage{
CheckScatter(
  data,
  x_col,
  y_col,
  scale = NULL,
  scale_factor = 10,
  sample_size = 100
)
}
\arguments{
\item{data}{A data frame or matrix containing numeric values.}

\item{x_col}{A string specifying the column name to be used for the x-axis.}

\item{y_col}{A string specifying the column name to be used for the y-axis.}

\item{scale}{Optional. A string specifying the transformation to apply: either \code{"log"} or \code{"arcsinh"}.
If \code{NULL}, no transformation is applied.}

\item{scale_factor}{A numeric value used as the base for log scaling or the divisor for arcsinh transformation. Default is 10.}

\item{sample_size}{An integer specifying the number of rows to randomly sample from \code{data} for plotting.
If \code{NULL}, all rows are used.}
}
\value{
A ggplot object showing the scatter plot of the selected columns, with optional transformation and sampling.
}
\description{
This function generates a scatter plot of two selected columns from a data frame or matrix.
It supports optional log or arcsinh scaling, and random sampling of rows for visualization.
}
\examples{
library(ggplot2)
set.seed(42)
df <- data.frame(A = rnorm(1000), B = rnorm(1000))
CheckScatter(df, "A", "B", scale = "log", scale_factor = 10, sample_size = 200)

}
