% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CompareSig.R
\name{CompareSig}
\alias{CompareSig}
\title{Compare Two Signal Signatures from a Matrix}
\usage{
CompareSig(mtx, sig1, sig2)
}
\arguments{
\item{mtx}{A matrix or data frame where rows represent signal signatures and columns represent channels.}

\item{sig1}{A string specifying the name of the first signature to compare.}

\item{sig2}{A string specifying the name of the second signature to compare.}
}
\value{
A ggplot object showing the signal profiles of the two selected signatures across channels.
If either signature is not found or contains non-numeric values, an error is raised.
}
\description{
This function compares two signal signatures (rows) from a matrix by checking their existence,
validating that all values are numeric, and plotting their signal profiles across channels.
}
\examples{
library(ggplot2)
library(tibble)
mtx <- matrix(runif(20), nrow = 2, ncol = 10)
rownames(mtx) <- c("Sig1", "Sig2")
colnames(mtx) <- paste0("Ch", 1:10)
CompareSig(mtx, "Sig1", "Sig2")

}
